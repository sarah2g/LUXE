Django==6.0.3
gunicorn==20.1.0
whitenoise==6.0.0
python-decouple==3.8
dj-database-url==1.0.0
psycopg2-binary==2.9.7

# Optional, keep for local formatting/sql parsing if needed
sqlparse==0.5.5

# Ensure pkg_resources is present at runtime (fixes ModuleNotFoundError in some PaaS envs)
# Pin a known setuptools release to avoid environment inconsistencies on PaaS
setuptools==82.0.1
wheel==0.40.0
